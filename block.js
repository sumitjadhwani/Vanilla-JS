// {....} is block in JS
//Below is valid example of block
{
}

//if else by default accepts only one executable statement
if(true) console.log(450);


//So, If we want to execute more statements we need to use BLOCK
if(true) {
	var x = 10;
  var y = 1;
  console.log(x);
  console.log(y);
}
